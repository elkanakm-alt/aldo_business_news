<div class="p-4 rounded-2xl shadow-lg text-white bg-gradient-to-r {{ $gradient }}">
    <div class="flex items-center justify-between">
        <div>
            <p class="text-sm">{{ $title }}</p>
            <p class="text-2xl font-bold">{{ $value }}</p>
        </div>
        <div class="text-3xl">{{ $icon }}</div>
    </div>
</div>